const styles = {
  header: {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
  },
  lang: {
    marginTop: '10px',
    background: (theme) => theme.background.secondaryButton,
    fontSize: '.8rem',
  },
};

export default styles;
